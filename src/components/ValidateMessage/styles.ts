import { css } from '@emotion/react'
import theme from '@/theme'

export const container = css`
  position: relative;
  display: block;
  padding-top: 12px;
  color: ${theme.aqua[100]};
  font-size: 12px;
`
