import { css } from '@emotion/react'

export const container = css`
  display: inline-block;
  margin: 10px 0;
  cursor: pointer;
  input {
    display: none;
  }
  label {
    vertical-align: middle;
    margin: 10px 3px;
  }
`
